import { FC } from 'react';
// import { useAppDispatch } from 'hooks';

import SectionStyled from './styles';

const Login: FC = () => {
  // const dispatch = useAppDispatch();
  return <SectionStyled>code here</SectionStyled>;
};

export default Login;
