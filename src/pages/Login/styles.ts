import styled from 'styled-components';

type Props = {};

export default styled.section<Props>`
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  .form-login {
    width: 350px;
    margin: auto;
  }
`;
